<Window x:Class="training.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:training"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="Button" x:Key="game_buttons">
            <Setter Property="FontSize" Value="24"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="game_buttons" Background="Beige" Grid.ColumnSpan="3" Orientation="Horizontal">
            <Button Width="100" Content="Save Game"  Margin="50,10"></Button>
            <Button Width="100" Content="Next Step" Margin="150,10"></Button>
            <Button Width="100" Content="Main Menu" Margin="50,10"></Button>

        </StackPanel>

        
        <TextBlock Grid.Column="1" Grid.Row="1" Background="LightBlue">

        </TextBlock>

        <!--<StackPanel x:Name="playerLeft" Grid.Row="1" />-->

        <TreeView x:Name="playerLeft" Grid.Row="1" ItemsSource="{Binding Games}">
            
        </TreeView>


        <StackPanel x:Name="palyerRight" Grid.Row="1" Grid.Column="2">
            
        </StackPanel>


    </Grid>


</Window>
